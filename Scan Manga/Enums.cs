namespace Scan_Manga;

public enum FullScreenMode
{
    AllPages = 0,
    ReadingOnly = 1,
    Disabled = 2,
}

public enum KeepScreenOnMode
{
    AllPages = 0,
    ReadingOnly = 1,
    ChargingOnly = 2,
    Disabled = 3
}

public enum PickerVariant
{
    Outlined,
    Filled,
    Text
}